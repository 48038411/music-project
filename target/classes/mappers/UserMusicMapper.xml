<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soft1851.music.mapper.UserMusicMapper">
    <!-- 新增用户   -->
    <insert id="likeMusic" parameterType="UserMusic">
        INSERT IGNORE INTO
            t_user_music(id,music_id,user_id)
        VALUES (#{id},#{musicId},#{userId})
    </insert>
    <delete id="batchDelete" parameterType="int">
        DELETE FROM t_user_music
        WHERE music_id IN
        <foreach collection="idList" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <insert id="batchInsert" parameterType="UserMusic">
        INSERT INTO t_user_music VALUES
        <foreach collection="userMusics" item="item" index="index" separator=",">
            (#{item.id},#{item.musicId},#{item.userId})
        </foreach>
    </insert>
</mapper>